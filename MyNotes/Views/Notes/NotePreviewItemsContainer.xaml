<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="MyNotes.Views.Notes.NotePreviewItemsContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:MyNotes.Views.Notes">
  <UserControl.Resources>
    <Style x:Key="GridViewItemContainerStyle"
           TargetType="GridViewItem"
           BasedOn="{StaticResource DefaultGridViewItemStyle}">
      <Setter Property="HorizontalContentAlignment"
              Value="Stretch" />
      <Setter Property="VerticalContentAlignment"
              Value="Stretch" />
    </Style>

    <ItemsPanelTemplate x:Key="GridViewItemsPanel_LayoutGrid">
      <ItemsWrapGrid Orientation="Horizontal" />
    </ItemsPanelTemplate>

    <ItemsPanelTemplate x:Key="GridViewItemsPanel_LayoutList">
      <ItemsStackPanel />
    </ItemsPanelTemplate>

    <DataTemplate x:Key="GridViewItemTemplate_LayoutGrid"
                  x:DataType="notevm:NoteViewModel"
                  xmlns:notevm="using:MyNotes.ViewModels.Notes">
      <local:NotePreviewGridItemContainer ViewModel="{x:Bind Mode=OneWay}"
                                          PreviewRtf="{x:Bind Preview, Mode=OneWay}"
                                          Margin="6" />
    </DataTemplate>
    <DataTemplate x:Key="GridViewItemTemplate_LayoutList"
                  x:DataType="notevm:NoteViewModel"
                  xmlns:notevm="using:MyNotes.ViewModels.Notes">
      <local:NotePreviewListItemContainer ViewModel="{x:Bind Mode=OneWay}"
                                          PreviewRtf="{x:Bind Preview, Mode=OneWay}"
                                          Margin="6" />
    </DataTemplate>
  </UserControl.Resources>
  
  <GridView x:Name="PreviewGridView"
            ItemsSource="{x:Bind ItemsSource, Mode=OneWay}"
            SelectionMode="None"
            AllowFocusOnInteraction="False"
            ScrollViewer.VerticalScrollMode="Enabled"
            ScrollViewer.VerticalScrollBarVisibility="Visible"
            ItemsPanel="{StaticResource GridViewItemsPanel_LayoutGrid}"
            ItemTemplate="{StaticResource GridViewItemTemplate_LayoutGrid}"
            ItemContainerStyle="{StaticResource GridViewItemContainerStyle}" />
</UserControl>
