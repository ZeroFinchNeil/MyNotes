<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="MyNotes.Views.Dialogs.ConfirmDeleteDialog"
               x:Uid="ConfirmDeleteDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               Style="{StaticResource DefaultContentDialogStyle}"
               DefaultButton="Close"
               PrimaryButtonText="Delete"
               CloseButtonText="Cancel"
               Title="{x:Bind SetTitle(ViewModel.TargetCategory)}">

  <StackPanel>
    <TextBlock Text="{x:Bind ViewModel.TargetName}" />

    <CheckBox x:Uid="/ConfirmDeleteDialog/DeletePermanentlyCheckBox"
              IsChecked="{x:Bind ViewModel.DeletePermanently, Mode=TwoWay}" />
  </StackPanel>
</ContentDialog>
